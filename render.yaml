services:
  - type: web
    name: mileagehc
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /data
    disk:
      name: mileage-data
      mountPath: /data
      sizeGB: 1
